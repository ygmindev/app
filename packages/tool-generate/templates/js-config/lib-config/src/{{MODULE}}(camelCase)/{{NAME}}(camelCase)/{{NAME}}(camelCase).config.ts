import { _{{NAME}}(camelCase)Config } from '@lib/config/{{MODULE}}(camelCase)/{{NAME}}(camelCase)/_{{NAME}}(camelCase).config';
import { {{NAME}}(camelCase)Config as config } from '@lib/config/{{MODULE}}(camelCase)/{{NAME}}(camelCase)/configs/{{NAME}}(camelCase).config';

export const {{NAME}}(camelCase)Config = _{{NAME}}(camelCase)Config(config);
