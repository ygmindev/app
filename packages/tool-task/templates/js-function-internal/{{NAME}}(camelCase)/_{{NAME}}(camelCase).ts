import {
type  _{{NAME}}(pascalCase)Model,
  _{{NAME}}(pascalCase)ParamsModel,
} from '@{{PATH}}(pathCase)/{{NAME}}(camelCase)/_{{NAME}}(camelCase).models';

export const _{{NAME}}(camelCase) = ({}: _{{NAME}}(pascalCase)ParamsModel): _{{NAME}}(pascalCase)Model => {
  return;
};
