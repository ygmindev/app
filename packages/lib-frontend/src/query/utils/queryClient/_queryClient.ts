import type { _QueryClientParamsModel } from '@lib/frontend/query/utils/queryClient/_queryClient.models';

export const _queryClient = async ({}: _QueryClientParamsModel): Promise<void> => {
  return;
};
