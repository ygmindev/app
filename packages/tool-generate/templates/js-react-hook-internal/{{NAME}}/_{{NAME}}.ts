import type {
  _{{NAME}}(pascalCase)Model,
  _{{NAME}}(pascalCase)ParamsModel,
} from '{{PATH}}/{{NAME}}/_{{NAME}}.models';

export const _{{NAME}} = (
  {}: _{{NAME}}(pascalCase)ParamsModel,
): _{{NAME}}(pascalCase)Model => {
  return {};
};
