---
import Layout from '../layouts/Layout.astro';
import { AdminHomePage } from '@lib/frontend/admin/pages/AdminHomePage/AdminHomePage';
import { InitialStateModel } from '@lib/shared/root/root.models';
import { USER } from '@lib/shared/user/user.constants';

const initialState: InitialStateModel = {
    [USER]: {
        currentUser: null,
    },
};
---

<Layout>
    <AdminHomePage client:load initialState={initialState} />
</Layout>
