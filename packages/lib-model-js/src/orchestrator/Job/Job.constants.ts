export const JOB_RESOURCE_NAME = 'Job';

export enum JOB_STATUS {
  CANCELLED = 'cancelled',
  FAIL = 'fail',
  RUNNING = 'running',
  STARTED = 'started',
  SUCCESS = 'success',
  UNKNOWN = 'unknown',
}
