import {
  type _SerializeCookieModel,
  type _SerializeCookieParamsModel,
} from '@lib/backend/http/utils/serializeCookie/_serializeCookie.models';

export type SerializeCookieParamsModel = _SerializeCookieParamsModel;

export type SerializeCookieModel = _SerializeCookieModel;
