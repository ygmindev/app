import { _parserConfig } from '@lib/config/locale/parser/_parser';
import { parserConfig as config } from '@lib/config/locale/parser/configs/parser';

export const parserConfig = _parserConfig(config);
