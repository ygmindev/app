import type {
  _SynchronizeModel,
  _SynchronizeParamsModel,
} from '@lib/shared/core/utils/synchronize/_synchronize.models';

export const _synchronize = ({}: _SynchronizeParamsModel): _SynchronizeModel => {
  return;
};
