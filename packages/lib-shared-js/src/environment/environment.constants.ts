export enum ENVIRONMENT {
  DEVELOPMENT = 'development',
  PRODUCTION = 'production',
  TEST = 'test',
}

export enum RUNTIME {
  AWS_LAMBDA = 'AWS_LAMBDA',
  CONTAINER = 'CONTAINER',
  OSX = 'OSX',
}
