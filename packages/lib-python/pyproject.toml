[build-system]
requires = ["poetry_core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "lib-python"
description = "lib-python"
version = "0.0.1"
license = "MIT"
authors = ["YG Min"]
packages = [{ include = "lib_python" }]

[tool.poetry.dependencies]
python = "~3.12"

[tool.poetry.group.dev.dependencies]
flake8 = "^7.1.1"

