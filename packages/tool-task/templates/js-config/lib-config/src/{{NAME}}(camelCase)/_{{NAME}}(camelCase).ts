import { type _{{NAME}}(pascalCase)ConfigModel, type {{NAME}}(pascalCase)ConfigModel } from '@lib/config/{{NAME}}(camelCase)/{{NAME}}(camelCase).models';

export const _{{NAME}}(camelCase) = ({}: {{NAME}}(pascalCase)ConfigModel): _{{NAME}}(pascalCase)ConfigModel => ({
});
