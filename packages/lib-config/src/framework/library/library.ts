import { _libraryConfig } from '@lib/config/framework/library/_library';
import { libraryConfig as config } from '@lib/config/framework/library/configs/library';

export const libraryConfig = _libraryConfig(config);
