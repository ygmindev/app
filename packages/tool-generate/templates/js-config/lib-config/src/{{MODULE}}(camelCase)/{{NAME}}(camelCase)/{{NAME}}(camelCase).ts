import type { {{NAME}}(pascalCase)ConfigModel } from '@lib/config/{{MODULE}}(pathCase)/{{NAME}}(camelCase)/_{{NAME}}(camelCase).models';

const {{NAME}}(camelCase)Config: {{NAME}}(pascalCase)ConfigModel = {
};

export default {{NAME}}(camelCase)Config;
