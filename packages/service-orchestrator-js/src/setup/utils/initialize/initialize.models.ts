import {
  type InitializeModel as InitializeBackendModel,
  type InitializeParamsModel as InitializeBackendParamsModel,
} from '@lib/backend/setup/utils/initialize/initialize.models';

export type InitializeParamsModel = InitializeBackendParamsModel;

export type InitializeModel = InitializeBackendModel;
