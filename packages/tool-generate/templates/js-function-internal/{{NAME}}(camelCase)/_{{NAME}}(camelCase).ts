import type {
  _{{NAME}}(pascalCase)Model,
  _{{NAME}}(pascalCase)ParamsModel,
} from '{{PATH}}/{{NAME}}(camelCase)/_{{NAME}}(camelCase).models';

export const _{{NAME}}(camelCase) = async ({}: _{{NAME}}(pascalCase)ParamsModel): Promise<_{{NAME}}(pascalCase)Model> => {
  return;
};
