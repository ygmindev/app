---
import Layout from '@app/web/layouts/Layout.astro';
import { SignInPage } from '@lib/frontend/auth/pages/SignInPage/SignInPage';
import { InitialStateModel } from '@lib/shared/root/root.models';
import { USER } from '@lib/shared/user/user.constants';

const initialState: InitialStateModel = {
    [USER]: {
        currentUser: null,
    },
};
---

<Layout>
    <SignInPage client:load initialState={initialState} />
</Layout>
