import {
  type _RunServerModel,
  type _RunServerParamsModel,
} from '@lib/backend/server/utils/runServer/_runServer.models';

export type RunServerParamsModel = _RunServerParamsModel;

export type RunServerModel = _RunServerModel;
