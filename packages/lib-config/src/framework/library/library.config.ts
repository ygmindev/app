import { _libraryConfig } from '@lib/config/framework/library/_library.config';
import { libraryConfig as config } from '@lib/config/framework/library/configs/library.config';

export const libraryConfig = _libraryConfig(config);
