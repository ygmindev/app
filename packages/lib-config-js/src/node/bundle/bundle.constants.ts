export enum BUNDLE_FORMAT {
  CJS = 'cjs',
  ESM = 'esm',
}

export enum BUNDLE_SOURCEMAP {
  INLINE = 'inline',
  OUTPUT = 'output',
}

export enum APP_TYPE {
  SERVER = 'server',
  TOOL = 'tool',
  WEB = 'web',
}
