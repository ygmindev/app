import { _{{NAME}}(camelCase)Config } from '@lib/config/{{MODULE}}(camelCase)/{{NAME}}(camelCase)/_{{NAME}}(camelCase)';
import { {{NAME}}(camelCase)Config as config } from '@lib/config/{{MODULE}}(camelCase)/{{NAME}}(camelCase)/configs/{{NAME}}(camelCase)';

export const {{NAME}}(camelCase)Config = _{{NAME}}(camelCase)Config(config);
